@model gestaoIpg.Models.Departamento

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <h2>Detalhes</h2>

    <div>
        <!--<h4>Departamento</h4>-->
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Tipo)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Tipo)
            </dd>
        </dl>
    </div>
    <div>
        <a class="btn btn-warning"asp-action="Edit" asp-route-id="@Model.DepartamentoId">Editar</a> |
        <a asp-action="Index">Voltar</a>
    </div>
</div>