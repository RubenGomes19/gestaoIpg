@model gestaoIpg.Models.Tarefa

@{
    ViewData["Title"] = "Details";
}


    <div class="container">
        <h2>Detalhes</h2>

        <div>
            
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.DescricaoTarefa)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.DescricaoTarefa)
                </dd>
                <dt>
                    @Html.DisplayNameFor(model => model.Funcionario)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.Funcionario.Nome)
                </dd>
            </dl>
        </div>
        <div>
            <a class="btn btn-warning" asp-action="Edit" asp-route-id="@Model.TarefaId">Editar</a> |
            <a asp-action="Index">Voltar</a>
        </div>
     </div>
