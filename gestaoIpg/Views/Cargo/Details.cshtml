@model gestaoIpg.Models.Cargo

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <div class="container">

        <h2>Detalhes</h2>

        <div>
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.NomeCargo)
                </dt>
                <dd>
                    @Html.DisplayFor(model => model.NomeCargo)
                </dd>
            </dl>
        </div>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.CargoId" class="btn btn-warning">Editar</a> |
            &nbsp;
            <a asp-action="Index" class="btn btn-link" style="color:black">Voltar</a>
        </div>
    </div>
